{
    "name": "penn-dining-agent",
    "version": "1.0.0",
    "description": "Penn Dining iMessage agent — hours, menus, and crowd-sourced reviews",
    "type": "module",
    "private": true,
    "scripts": {
        "start": "node --env-file=.env --import tsx bot.ts",
        "dev": "node --env-file=.env --watch --import tsx bot.ts",
        "type-check": "tsc --noEmit"
    },
    "dependencies": {
        "@google/genai": "^1.49.0",
        "@photon-ai/imessage-kit": "file:..",
        "googleapis": "^144.0.0",
        "twilio": "^5.13.1"
    },
    "devDependencies": {
        "@types/node": "^20.0.0",
        "tsx": "^4.19.0",
        "typescript": "^5.3.0"
    },
    "engines": {
        "node": ">=18.0.0"
    }
}
